/**
 * UIST intentionally has no custom listable components. DSpace 10's unthemed
 * implementations remain authoritative for item, community, collection, and
 * browse-result rendering.
 */
export const LISTABLE_COMPONENTS = [];
